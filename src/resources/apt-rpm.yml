modules:
  - type: move
    body:
      target: /etc/apt
      destination: /usr/share/apt
      create-link: true
  - type: move
    body:
      target: /var/lib/rpm
      destination: /usr/var/lib/rpm
      create-link: true
  - type: move
    body:
      target: /var/lib/apt
      destination: /usr/var/lib/apt
      create-link: true
  - type: move
    body:
      target: /var/cache/apt
      destination: /usr/var/cache/apt
      create-link: true
  - type: move
    body:
      target: /var/lib/apm
      destination: /usr/var/lib/apm
      create-link: true
