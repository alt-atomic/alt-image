modules:
  - name: Set locale
    type: copy
    body:
      source: files/locale.conf
      destination: /etc/locale.conf
      replace: true

  - type: copy
    body:
      source: files/locale.conf
      destination: /etc/sysconfig/i18n
      replace: true
