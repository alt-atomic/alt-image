modules:
  - name: Install task bootupd
    type: repos
    body:
      tasks:
        - 411418

  - name: Build and install bootupd
    type: packages
    body:
      install:
        - bootupd
      update: true
