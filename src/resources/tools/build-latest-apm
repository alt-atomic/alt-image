#!/usr/bin/bash -e

# USE ONLY FOR TESTS, SCRIPTS DONT CLEAN UP DEVEL PACKAGES

apt-get update
apt-get dist-upgrade -y

apt-get install -y golang libapt-devel gettext-devel git meson gcc-c++ "pkgconfig(systemd)"

cd $(mktemp -d)
git clone https://altlinux.space/alt-atomic/apm.git
cd apm

meson setup _build --wipe --prefix /usr
meson install -C _build
