modules:
  - type: remove
    body:
      targets:
        - /etc/fstab
        - /var/cache/apt
  - type: remove
    body:
      targets:
        - /boot
      inside: true
  - type: mkdir
    body:
      target: /var/cache/apt/archives/partial
      perm: 660
