modules:
  - type: remove
    body:
      targets:
        - /etc/fstab
        - /usr/etc
  - type: remove
    body:
      targets:
        - /boot
      inside: true
  - type: shell
    body:
      command: /usr/libexec/apt-get clean
