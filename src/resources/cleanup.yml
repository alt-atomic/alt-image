modules:
  - type: remove
    body:
      targets:
        - /etc/fstab
        - /var/cache/apt
  - type: remove
    body:
      targets:
        - /boot
      inside: true
  - type: shell
    body:
      command: /usr/libexec/apt-get clean
