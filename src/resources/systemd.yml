modules:
  - type: systemd
    body:
      targets:
        - ostree-remount
        - NetworkManager
        - libvirtd
        - chrony
        - docker.socket
        - podman.socket
        - sync-users.service
        - sync-directory.service
        - tmp.mount
      enabled: true
