# image.yml exists in order to keep the kernel version in one place
env:
  KERNEL_FLAVOUR: 6.18

modules:
- name: Building ${{ Env.BUILD_TYPE }}
  type: include
  body:
    targets:
    - image-${{ Env.BUILD_TYPE }}.yml
