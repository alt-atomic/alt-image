# grub 2.14 blsuki defaults to (root)/loader/entries (separate /boot partition).
# Without a separate /boot, entries are at (root)/boot/loader/entries; point there.
# --enable-fallback keeps the separate-/boot layout working.
blscfg --path /boot/loader/entries --enable-fallback
