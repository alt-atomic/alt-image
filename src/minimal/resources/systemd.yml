modules:
  - type: systemd
    body:
      targets:
        - ostree-remount
        - NetworkManager
        - chrony
        - podman.socket
        - sync-users.service
#        - sync-directory.service
        - locked-db-migrate.service
        - fstrim.timer
      enabled: true
