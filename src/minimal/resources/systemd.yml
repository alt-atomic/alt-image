modules:
  - type: systemd
    body:
      targets:
        - bootloader-update
        - chrony
        - fstrim.timer
        - locked-db-migrate.service
        - NetworkManager
        - ostree-remount
        - podman.socket
        - sync-users.service
      enabled: true
